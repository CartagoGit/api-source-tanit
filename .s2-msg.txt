feat(graphql): a00015/S2 adapter AST-based replaces regex

- packages/frameworks/scanners/graphql-embedded.adapter.ts:
  pure adapter collectEmbeddedSdl(templates, options?) → SDL
  strings. Filtra por tag (default ["gql", "graphql"]).
  El scanner lo alimenta desde collectTaggedTemplates(projectRoot).
- tests/frameworks/graphql-embedded-adapter.spec.ts: 9 tests
  cubren positivo real, graphql\`...\` con default-tags, tag filter
  (tag distinto excluido), vacío pasa pas tal cual, tags custom
  ['graphql'], tags custom ['gql','css'], 5-uso, mezcla de tags,
  input vacío.

a00015 S2/3.