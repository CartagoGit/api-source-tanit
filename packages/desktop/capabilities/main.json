{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal permissions for the Tanit desktop window",
  "windows": ["principal"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:default"
  ]
}