---
title: "Auditoria completa 2026-08-08 — estado real, puntuacion por areas y backlog de excelencia"
date: 2026-08-08
status: vigente
track: export-to-postman
---

# Auditoria completa 2026-08-08 — estado real, puntuacion por areas y backlog de excelencia

Esta es la auditoria vigente del repositorio. Sustituye como foto actual a la de 2026-08-06: aquella sigue siendo valiosa como registro historico, pero ya no describe con precision el arbol de trabajo de hoy.

La conclusion corta es esta: el proyecto tiene una base tecnica mucho mejor que la media de herramientas CLI de este tipo, pero aun no esta en excelencia sostenida. El producto funciona, el paquete se valida de punta a punta y la seguridad esta bastante bien cerrada; lo que sigue flojo es la alineacion entre contrato, configuracion, documentacion, superficie MCP y restos de arquitectura heredada.

---

## 1. Estado medido en esta pasada

### Checks ejecutados

- `mcp-vertex_security_security_audit` -> limpio: 0 secretos, 0 CVE, 0 hallazgos de licencias.
- `get_errors` -> limpio en el workspace.
- `bun run validate:package` -> verde.
- `bun run lint:proposals` -> verde.
- `bun run lint:mcp-surface` -> verde.
- `bun run validate` -> rojo por `lint:api`.
- `mcp-vertex_overview { compact: true }` -> 8 `configIssues` en `search` y `conventions`.
- `debt_scan` -> 9 TODO, todos en la plantilla generada por `init`.

### Lo que esta bien y conviene no romper

- El paquete instalable funciona de punta a punta.
- La superficie MCP existente ya tiene `inputSchema` y `outputSchema` en sus 4 tools.
- Hay gates reales, no solo prosa: naming, boundaries, docs, secrets, SAST, mcp-surface, package validation y proposals drift.
- El proyecto ya piensa en calidad como sistema, no como gesto puntual.

### Lo que demuestra que aun hay drift

- `docs/API.md` puede quedar atras respecto al codigo publico.
- `mcp-vertex.config.json` sigue apuntando a carpetas que no existen.
- `CONTRIBUTING.md` cita `docs/extension-contract.md` como fuente de verdad y ese archivo no existe.
- `paths.service.ts` sigue siendo un singleton con cola de serializacion.
- Solo 4 tools MCP exponen una superficie CLI mucho mas amplia.
- No existe gate de cobertura cuantitativa: hay muchos tests, pero no un numero objetivo de lineas o ramas que impida zonas ciegas.

---

## 2. Marcador global

| Area | Nota | Lectura corta |
|---|---:|---|
| Correccion funcional del producto | 8.8/10 | Genera artefactos validos y el paquete se deja instalar y usar |
| Arquitectura y reentrancia | 6.3/10 | Buena separacion por capas, pero sigue habiendo estado global heredado |
| Superficie MCP y contratos de agente | 7.6/10 | El contrato existe; la cobertura funcional de tools aun es estrecha |
| Documentacion y contrato de trabajo | 5.8/10 | Mucha documentacion util, pero con drift real en sitios criticos |
| Testing cualitativo | 8.2/10 | Suite amplia, tests por secciones y gates especificos |
| Testing cuantitativo | 6.1/10 | Sin coverage de lineas o ramas ni umbrales que vigilen regresiones silenciosas |
| Seguridad operacional | 8.9/10 | Secrets, SAST y CVE bien cubiertos; falta cerrar contencion de salidas en MCP |
| Rendimiento y escalabilidad | 7.2/10 | Hay benchmark y pensamiento de coste, pero sin presupuesto vivo ni concurrencia limpia |
| Packaging y distribucion | 8.0/10 | `validate:package` y release-binaries existen; npm sigue dependiendo del dueño |
| UX y DX de CLI | 7.1/10 | Muchos comandos y buen alcance; lenguaje y semantica aun no son uniformes |
| Gobernanza de calidad | 7.7/10 | Hay muchos gates, pero aun sobreviven drifts importantes |

**Nota compuesta actual: 7.4/10.**

No es una nota mala. De hecho es una base fuerte. Lo que impide llamarlo "10/10" no es que el producto no funcione: es que todavia hay varios sitios donde el repo dice una cosa y el arbol real hace otra, y eso siempre acaba volviendo como deuda operativa.

---

## 3. Valoracion por aspecto

### 3.1 Producto core y frameworks — 8.8/10

**Fortalezas**

- La propuesta de valor esta clara y ya funciona de verdad: escanear un proyecto API y producir colecciones Postman y environments validos.
- `validate:package` demuestra el camino completo: empaquetar, instalar, ejecutar el binario y validar el artefacto de salida.
- El README describe 21 frameworks y el repo tiene ejemplos y fixtures para esa ambicion.

**Debilidades**

- `enrich` sigue siendo un resto historico: un comando visible del producto que hoy no esta a la altura del resto de la superficie.
- `scan` y `summary` siguen leyendo `process.env` o `process.argv` por su lado, asi que dos comandos de la misma familia no comparten una disciplina comun de contexto.

**Mejora o propuesta**

- `r00004` para decidir si `enrich` sobrevive o se retira.
- `r00005` nueva para cerrar de verdad la migracion fuera del singleton de rutas y del ambiente global.

### 3.2 Arquitectura y reentrancia — 6.3/10

**Fortalezas**

- El arbol ya esta bastante bien separado por capas: `core`, `frameworks`, `cli`, `ui`, `plugin`.
- Hay linter de boundaries para impedir cruces impropios.

**Debilidades**

- `projects/core/discovery/paths.service.ts` sigue cacheando rutas a nivel de proceso (`let cache: Discovered | null = null`).
- El mismo fichero mantiene una `queue` para serializar llamadas concurrentes, lo que confirma que la reentrancia no esta resuelta en la raiz, solo amortiguada.
- `summary.script.ts`, `scan.script.ts`, `push.script.ts` y partes de `project-loader.service.ts` siguen dependiendo de `process.env` o de `process.argv` como mecanismo de contexto.

**Lectura**

El proyecto ya sabe que ese singleton es deuda: el propio codigo lo explica. Eso es bueno. Lo que falta es cerrarlo por completo para que el MCP y los consumidores de vida larga dejen de pagar el impuesto de estado global.

**Mejora o propuesta**

- `r00005` nueva.

### 3.3 Superficie MCP y contratos de agente — 7.6/10

**Fortalezas**

- Los 4 tools existentes (`generate`, `summary`, `validate`, `test`) ya declaran `outputSchema`.
- `lint:mcp-surface` existe y hoy pasa en verde.
- `tests/cli/mcp-surface.spec.ts` ya comprueba parte del contrato.

**Debilidades**

- La propuesta `x00001` seguia describiendo una deuda que el arbol actual ya ha corregido en parte; eso es drift de backlog.
- El plugin sigue exponiendo una fraccion pequena del CLI: un agente no puede pedir por MCP `check`, `list`, `stats`, `scan`, `push` o `init` en forma estructurada.
- `push` y `open` requieren decisiones conscientes de superficie: no todo comando merece MCP por defecto, y eso hay que dejarlo escrito.

**Mejora o propuesta**

- Actualizar `x00001` para que deje de perseguir `outputSchema` ausente y pase a perseguir amplitud de superficie y pruebas integradas del contrato.

### 3.4 Documentacion y contrato de trabajo — 5.8/10

**Fortalezas**

- Hay mucha documentacion: README, INSTALL, FRAMEWORKS, POSTMAN, bootstrap, propuestas y changelog.
- El repo ya tiene gates de docs y de proposals.

**Debilidades**

- `AGENT-BOOTSTRAP.md` y `mcp-vertex.config.json` ya estaban denunciados por `d00001` y siguen siendo drift pendiente.
- `CONTRIBUTING.md` cita `docs/extension-contract.md` como fuente de verdad y ese archivo no existe hoy.
- El README sigue apuntando a la auditoria 2026-08-06 como referencia de estado, cuando ya no es la foto vigente.

**Mejora o propuesta**

- Ampliar `d00001` con el drift de `CONTRIBUTING.md` y la referencia de README al documento de auditoria vigente.

### 3.5 Testing cualitativo — 8.2/10

**Fortalezas**

- La suite esta organizada por secciones y proyectos de Vitest.
- Existen tests del paquete, tests por seccion, gates de examples, package validation y lints con tests propios.
- La propia historia del repo muestra aprendizaje real a partir de bugs cazados por tests malos y luego corregidos.

**Debilidades**

- `t00001` sigue siendo necesario porque varios comandos importantes aun no se ejercitan end-to-end como comandos.
- El parser YAML escrito a mano sigue mereciendo pruebas mas agresivas y generativas.

**Mejora o propuesta**

- `t00001` sigue siendo prioritaria.

### 3.6 Testing cuantitativo — 6.1/10

**Fortalezas**

- Hay muchas pruebas y varias puertas de entrada al comportamiento real.

**Debilidades**

- No hay configuracion de coverage en `vitest.config.ts`.
- No hay script de coverage en `package.json`.
- CI no publica artefactos ni umbrales de lineas o ramas.
- El proyecto puede tener 1.800 tests y aun asi dejar una zona entera sin tocar, y hoy no hay un numero que lo haga visible.

**Mejora o propuesta**

- `t00002` nueva para coverage cuantitativa por scopes y baseline gobernable.

### 3.7 Seguridad operacional — 8.9/10

**Fortalezas**

- El security audit sale limpio.
- Hay `lint:secrets`, `lint:sast` y `security:audit` dentro del flujo del repo.
- El caso de publicar a Postman esta ya pensado para no filtrar la API key por defecto.

**Debilidades**

- La contencion de rutas de salida aun no esta cerrada para la superficie MCP.
- `push` depende de secreto de usuario y sigue siendo una superficie donde el contrato de error y el saneamiento tienen que seguir estrechos.

**Mejora o propuesta**

- `x00003` sigue siendo necesaria.

### 3.8 Rendimiento y escalabilidad — 7.2/10

**Fortalezas**

- Existe un benchmark dedicado (`bench:scan`) y su razon de ser esta bien justificada.
- El repo ya sabe medir antes de prometer mejoras grandes.

**Debilidades**

- El benchmark no forma parte del gobierno habitual del repo.
- La concurrencia del contexto de rutas no esta resuelta de raiz: se serializa.

**Mejora**

- Cerrar `r00005` mejora mas la escalabilidad real del MCP que cualquier tuning prematuro del benchmark.

### 3.9 Packaging y distribucion — 8.0/10

**Fortalezas**

- `validate:package` existe y aporta mucha confianza real.
- Hay workflow de binarios para releases.
- El `publishConfig` esta preparado.

**Debilidades**

- El publish real a npm sigue dependiendo del dueño del repo y de credenciales, asi que la parte irreversible del ciclo no esta automatizada aqui.
- La UX de escritorio sigue siendo roadmap, no producto.

**Mejora o propuesta**

- `f00001` para experiencia sin terminal.
- No hace falta propuesta nueva de npm publish: `p00008` ya dejo claro que la accion final es manual del dueño.

### 3.10 UX y DX de CLI — 7.1/10

**Fortalezas**

- El CLI ya cubre descubrir, generar, validar, resumir, abrir, empujar y preparar configuracion.
- Hay alias binarios y trabajo previo de naming.

**Debilidades**

- El lenguaje de salida sigue mezclado segun el comando.
- La semantica de algunos comandos no esta al mismo nivel de claridad que el nucleo (`enrich` es el ejemplo principal).
- La herramienta sigue siendo terminal-first, no user-first.

**Mejora o propuesta**

- `r00003`, `r00004` y `f00001`.

### 3.11 Gobernanza y gates — 7.7/10

**Fortalezas**

- El repositorio tiene mas disciplina automatizada que la mayoria de proyectos de su tamano.
- `lint:proposals`, `lint:docs`, `lint:mcp-surface`, `validate:package` y los lints de seguridad muestran madurez.

**Debilidades**

- Que `bun run validate` caiga hoy por `docs/API.md` desincronizado es una prueba de salud del gate, pero tambien una prueba de que el flujo aun depende de pasos humanos no siempre absorbidos por el proceso.
- Que `mcp-vertex_overview` siga viendo `configIssues` confirma que la configuracion critica del propio host no esta aun bajo control total.

**Mejora o propuesta**

- `a00002`, `d00001`, `x00002` y `t00002`.

---

## 4. Hallazgos confirmados y su via de cierre

| ID | Severidad | Hallazgo | Estado | Propuesta |
|---|---|---|---|---|
| H1 | Alta | `docs/API.md` se desincroniza de la superficie publica | abierto | `a00002` + `x00002` |
| H2 | Alta | `search` y `conventions` apuntan a roots inexistentes | abierto | `d00001` |
| H3 | Alta | `paths.service.ts` sigue siendo singleton con cola global | abierto | `r00005` nueva |
| H4 | Media | `CONTRIBUTING.md` referencia un contrato muerto (`docs/extension-contract.md`) | abierto | `d00001` ampliada |
| H5 | Media | `x00001` ya no refleja la deuda real del MCP surface | abierto | `x00001` actualizada |
| H6 | Media | No hay coverage cuantitativa ni umbrales por scope | abierto | `t00002` nueva |
| H7 | Media | `enrich` sigue siendo deuda de superficie de producto | abierto | `r00004` |
| H8 | Media | La salida del CLI sigue mezclando idiomas y tono | abierto | `r00003` |
| H9 | Media | La contencion de rutas de salida en contexto MCP no esta cerrada | abierto | `x00003` |
| H10 | Baja | La publicacion real en npm sigue siendo un paso humano del dueño | aceptado | sin propuesta nueva |

---

## 5. Propuestas nuevas o actualizadas en esta pasada

### Nuevas

- `r00005` — cerrar la migracion fuera del singleton de paths y `process.env`.
- `t00002` — cobertura cuantitativa por scopes y baseline gobernable.

### Ampliadas o reenfocadas

- `d00001` — ahora debe cubrir tambien el drift de `CONTRIBUTING.md` y la referencia al documento de auditoria vigente.
- `x00001` — deja de perseguir `outputSchema` ausente y pasa a centrarse en la amplitud de la superficie MCP y sus pruebas integradas.
- `a00002` — incorpora estas dos propuestas nuevas y el orden real del backlog.

---

## 6. Orden recomendado para llegar a excelencia

1. Cerrar `x00002` y dejar `bun run validate` verde en arbol limpio.
2. Ejecutar `d00001` para alinear bootstrap, config MCP, CONTRIBUTING y enlaces.
3. Cerrar `x00003` para endurecer la superficie MCP que escribe en disco.
4. Ejecutar `r00005` para matar el remanente de estado global.
5. Reenfocar y ejecutar `x00001` para ampliar la superficie MCP util a agentes.
6. Ejecutar `r00004` y `r00003` para limpiar semantica y lenguaje del CLI.
7. Ejecutar `t00001` y `t00002` para cerrar tanto huecos funcionales como huecos cuantitativos.
8. Empujar `f00001` cuando se quiera subir la experiencia de uso final.

---

## 7. Criterio de excelencia realista

Este proyecto no necesita mas capas ceremoniales para acercarse a un 10.5/10. Necesita tres cosas muy concretas:

- que el contrato y el codigo no se contradigan,
- que el contexto deje de vivir en estado global,
- y que la calidad deje de medirse solo por cantidad de tests y pase a medirse tambien por coverage y por superficie real expuesta.

La buena noticia es que casi toda la deuda importante ya esta localizada. La mala es que varias piezas criticas siguen en ese estado peligroso de "ya sabemos cual es el problema" pero aun no "vive cerrada por un gate".

Cuando esas propuestas esten cerradas, la base del producto si estara cerca de la excelencia sostenida.

*** Add File: /home/cartago/_projects/export-to-postman/docs/mcp-vertex/proposals/ready/r00005-cerrar-la-migracion-fuera-del-singleton-de-paths-y-process-env.md
---
id: r00005
title: "Cerrar la migracion fuera del singleton de paths y process.env"
kind: refactor
status: ready
type: proposal
track: export-to-postman
date: 2026-08-08
related:
  - p00017
  - a00002
---

# r00005 — Cerrar la migracion fuera del singleton de paths y process.env

## Goal

Que el contexto del proyecto deje de vivir en estado global de proceso, para que el CLI, el MCP y cualquier consumidor de vida larga puedan operar sin cola global, sin carreras y sin ramas especiales por comando.

## why

La refactorizacion de `p00017` mejoro mucho la situacion, pero no la cerro. `projects/core/discovery/paths.service.ts` sigue diciendo en su propia cabecera que se prefiera `resolveProjectContext()` en codigo nuevo, y a la vez mantiene `let cache: Discovered | null = null`, una `queue` para serializar llamadas concurrentes y `withScopedPaths()` para salvar el estado global mientras ocho servicios y varios scripts aun dependen de el. Eso ya no es deuda teorica: es deuda observada y documentada por el propio codigo. `summary.script.ts` sigue resolviendo `--project-root` con `process.env.POSTMAN_PROJECT_ROOT` o cwd, `scan.script.ts` mezcla `projectRootFlag ?? process.env.POSTMAN_PROJECT_ROOT ?? projectRoot()`, `push.script.ts` llama a `projectRoot()` del singleton, y `project-loader.service.ts` sigue leyendo `POSTMAN_CONFIG` y `POSTMAN_EXAMPLE` directamente.

La consecuencia no es solo estetica. Mientras el contexto del proyecto viva ahi:

- el MCP paga una cola global para evitar carreras,
- dos peticiones concurrentes no pueden fiarse de un contexto explicito de punta a punta,
- y cada comando conserva pequeñas diferencias de semantica en como resuelve su raiz, su config o sus flags.

## non-goals

- Reescribir de una vez todos los consumers legacy sin una ruta de migracion.
- Prohibir `process.env` en entrypoints donde un secreto o una bandera de shell si son parte legitima de la interfaz externa.
- Cambiar el comportamiento visible del producto mas alla de hacerlo consistente.

## Slices

- global_gate: e2e

### S1 — Un solo lector de contexto para los entrypoints que aun divergen
- **Status**: pending
- **Files**: `projects/core/discovery/project-context.service.ts`, `projects/core/discovery/project-loader.service.ts`, `projects/cli/commands/summary.script.ts`, `projects/cli/commands/scan.script.ts`, `projects/cli/commands/push.script.ts`, `tests/core/project-context.service.spec.ts`
- **Gate**: type
- acceptance:
  - "`summary`, `scan` y `push` dejan de resolver `projectRoot` cada uno a su manera"
  - "`project-loader.service.ts` deja de leer `POSTMAN_CONFIG` y `POSTMAN_EXAMPLE` por libre si el contexto ya lo sabe"
  - "La politica de precedencia de flags y env queda escrita una vez"

### S2 — Sacar al pipeline y a los comandos del singleton caliente
- **Status**: pending
- **DependsOn**: [S1]
- **Files**: `projects/core/discovery/paths.service.ts`, `projects/core/discovery/generation.pipeline.ts`, `projects/core/discovery/project-context.service.ts`, `projects/cli/commands/generate.script.ts`, `projects/cli/commands/watch.script.ts`, `tests/core/scoped-paths.service.spec.ts`
- **Gate**: e2e
- acceptance:
  - "La ruta feliz del producto deja de depender de `withScopedPaths()` y de la `queue` global"
  - "`paths.service.ts` queda como shim legacy minimo o desaparece donde ya no aporte valor"
  - "Dos ejecuciones concurrentes sobre proyectos distintos no se pisan"

### S3 — Candado contra la recaida
- **Status**: pending
- **DependsOn**: [S2]
- **Files**: `scripts/gates/lint-project-context.script.ts`, `tests/cli/project-context-boundary.spec.ts`, `package.json`
- **Gate**: lint
- acceptance:
  - "El lint falla si aparece un nuevo acceso directo a `POSTMAN_PROJECT_ROOT` o `projectRoot()` del singleton en capas donde ya debe entrar contexto explicito"
  - "Se comprueba metiendo uno y viendo romper el gate"
  - "La excepcion legitima de entrypoints queda declarada, no inferida"

## acceptance

- `summary`, `scan` y `push` dejan de resolver `projectRoot` cada uno a su manera
- `project-loader.service.ts` deja de leer `POSTMAN_CONFIG` y `POSTMAN_EXAMPLE` por libre si el contexto ya lo sabe
- La politica de precedencia de flags y env queda escrita una vez
- La ruta feliz del producto deja de depender de `withScopedPaths()` y de la `queue` global
- `paths.service.ts` queda como shim legacy minimo o desaparece donde ya no aporte valor
- Dos ejecuciones concurrentes sobre proyectos distintos no se pisan
- El lint falla si aparece un nuevo acceso directo a `POSTMAN_PROJECT_ROOT` o `projectRoot()` del singleton en capas donde ya debe entrar contexto explicito
- Se comprueba metiendo uno y viendo romper el gate
- La excepcion legitima de entrypoints queda declarada, no inferida

*** Add File: /home/cartago/_projects/export-to-postman/docs/mcp-vertex/proposals/ready/t00002-cobertura-cuantitativa-y-presupuesto-de-rendimiento.md
---
id: t00002
title: "Cobertura cuantitativa por scopes y presupuesto basico de rendimiento"
kind: test
status: ready
type: proposal
track: export-to-postman
date: 2026-08-08
related:
  - t00001
  - a00002
---

# t00002 — Cobertura cuantitativa por scopes y presupuesto basico de rendimiento

## Goal

Que la calidad de test deje de medirse solo por cantidad de specs o por que `bun run test` salga verde, y pase a incluir dos numeros gobernables: cobertura de lineas y ramas por scope y una baseline minima para el tiempo de scan sintetico.

## why

Hoy el repo tiene una suite grande y valiosa, pero ninguna metrica cuantitativa que diga que zonas toca esa suite. `vitest.config.ts` no define coverage, `package.json` no tiene script de coverage y CI no publica artefactos ni umbrales. Eso deja un hueco peligroso: el proyecto puede tener 1.800 tests y aun asi mover una capa entera a rojo semantico sin que se vea en un porcentaje. `t00001` cubre la mitad funcional del problema — comandos sin test y parser YAML sin red —, pero no la mitad cuantitativa.

Hay un caso parecido en rendimiento. El repo ya tiene `bench:scan` y su propia propuesta historica demuestra que medir antes de prometer ahorra trabajo inutil. Pero esa medicion hoy es una herramienta suelta, no una baseline vigilada. No hace falta volverla una puerta dura y flaky en cada PR; si hace falta, como minimo, poder ver si el scan se ha degradado mucho sin enterarse semanas despues.

## non-goals

- Perseguir 100% global de coverage a martillazos.
- Meter un benchmark duro y fragil en cada PR si la maquina de CI no lo soporta.
- Duplicar `t00001`: esta propuesta mide, la otra amplia superficie funcional probada.

## Slices

- global_gate: test

### S1 — Coverage por scope en Vitest
- **Status**: pending
- **Files**: `vitest.config.ts`, `projects/plugins/mcp-vertex_expostman/vitest.config.ts`, `package.json`
- **Gate**: test
- acceptance:
  - "Se añade un script dedicado de coverage por scope"
  - "La salida distingue al menos `core`, `frameworks`, `cli`, `e2e` y `plugin`"
  - "La herramienta de coverage se integra sin romper la ejecucion habitual de `bun run test`"

### S2 — Umbrales y baseline realistas
- **Status**: pending
- **DependsOn**: [S1]
- **Files**: `scripts/gates/coverage-baseline.constant.ts`, `scripts/gates/check-coverage.script.ts`, `tests/cli/coverage-gate.spec.ts`, `package.json`
- **Gate**: lint
- acceptance:
  - "Cada scope tiene un suelo explicito, no un 100% ficticio"
  - "El gate falla si una regresion baja de ese suelo"
  - "La baseline se puede subir deliberadamente cuando mejore la suite, no por accidente"

### S3 — Presupuesto minimo de rendimiento para `bench:scan`
- **Status**: pending
- **DependsOn**: [S2]
- **Files**: `scripts/gates/bench-scan.script.ts`, `scripts/gates/check-scan-budget.script.ts`, `.github/workflows/validate.yml`
- **Gate**: none
- acceptance:
  - "Existe una comprobacion reproducible que alerta de degradaciones groseras del scan sintetico"
  - "La politica deja claro si ese presupuesto es bloqueante o solo informativo en CI"
  - "El repo deja escrita la razon del umbral, para que no se convierta en numero magico"

## acceptance

- Se añade un script dedicado de coverage por scope
- La salida distingue al menos `core`, `frameworks`, `cli`, `e2e` y `plugin`
- La herramienta de coverage se integra sin romper la ejecucion habitual de `bun run test`
- Cada scope tiene un suelo explicito, no un 100% ficticio
- El gate falla si una regresion baja de ese suelo
- La baseline se puede subir deliberadamente cuando mejore la suite, no por accidente
- Existe una comprobacion reproducible que alerta de degradaciones groseras del scan sintetico
- La politica deja claro si ese presupuesto es bloqueante o solo informativo en CI
- El repo deja escrita la razon del umbral, para que no se convierta en numero magico