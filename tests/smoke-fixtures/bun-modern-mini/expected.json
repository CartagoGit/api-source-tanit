{
  "framework": "express",
  "routes": [
    { "method": "DELETE", "uri": "/api/users/:id" },
    { "method": "GET",    "uri": "/api/users" },
    { "method": "GET",    "uri": "/api/users/:id" },
    { "method": "GET",    "uri": "/health" },
    { "method": "POST",   "uri": "/api/users" }
  ],
  "_x00035": {
    "comment": "Smoke fixture adicional: este caso verifica que el lockfile moderno (bun.lock, texto) no rompe la detección. Los routes deben coincidir con los de express-mini (mismo server.js); lo nuevo es que aquí existe bun.lock.",
    "lockfile_expected_evidence": {
      "artifact": "bun.lock",
      "weight": 0.15
    }
  }
}
