refactor(graphql): a00015/S3 eliminar regex sobre source crudo

- packages/frameworks/scanners/graphql.scanner.ts: scanner ahora
  consume collectEmbeddedSdl(collectTaggedTemplates(root)); el
  regex (?:gql|graphql)\s*` y la función extractEmbeddedSdl() se
  borran. La detección de .graphql/.gql queda igual.
- tests/frameworks/graphql-audit-2026-09-04.spec.ts: P1 #5 test
  ahora consume collectEmbeddedSdl(collectTaggedTemplatesFromSource)
  — exactamente el flujo que el scanner aplica.
- All embedded-Sdl extraction now goes through the AST visitor
  (S1 + S2). No regex reads of .ts source remain in the scanner.
- lint:regex-state green: ningún regex module-level con flag `g`
  en scanner.ts; el resto (sin flag) son sanitizaciones locales.

a00015 S3/3.